# PostgreSQL Configuration
POSTGRES_HOST=localhost
POSTGRES_PORT=5433
POSTGRES_DB=fantasy_sports_db
POSTGRES_USER=fantasy_user
POSTGRES_PASSWORD=fantasy_dev_password
POSTGRES_PORT=5432

# Vercel keys
# VERCEL_TOKEN (kept in GitHub Secrets)
# VERCEL_TEAM_ID (kept in GitHub Secrets)
# VERCEL_PROJECT_ID (kept in GitHub Secrets)